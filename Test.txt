PR 테스트 용 파일 입니다.
1. clone
2. cd 해당 파일로 이동
3. git reomte 로 헤당 레파지토리랑 연결됐는지 확인
4. git add Test.txt
5. git push origin main

파일 삭제
1. gir rm Test.txt - 깃 저장소 뿐 아니라 폴더 안에서도 삭제 됨
2. commit
3.git push origin main

브랜치 새로 만들기
git switch -c 브랜치이름

브랜치 바꾸기
git switch main(메인 브랜치로 바꾸거 싶을때)